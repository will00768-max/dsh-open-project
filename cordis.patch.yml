# Mount the dsh-open-with plugin. Add this to your profile's cordis.patch.yml
# (or the Web bundle used by the running deployment), then install the package:
#
#   dsh plugin --profile <name> add dsh-open-with
#
# and add the row below:
#
- insert:
    - id: open-with
      name: dsh-open-with
