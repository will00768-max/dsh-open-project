# Mount the dsh-open-project plugin by installing from GitHub with:
#
#   dsh plugin --profile web add github:will00768-max/dsh-open-project
#
# (from the DeepSeek Harness source: pnpm dsh plugin --profile web add ...).
# The row below is what gets inserted into the profile's cordis.patch.yml:
#
- insert:
    - id: open-with
      name: dsh-open-project
